export default function Loading() {
  return (
    <div
      style={{
        fontFamily: "system-ui",
        color: "rgb(2 132 199)",
        minHeight: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Loading ...
    </div>
  )
}
